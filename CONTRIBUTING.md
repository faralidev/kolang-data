# مشارکت در کلنگ‌دیتا (kolang-data)

این مخزن منبع حقیقی داده‌های زبانی کلنگ است. هر تغییری در داده‌ها باید از همین‌جا انجام شود و سپس به مخازن ویرایشگر همگام‌سازی شود.

## افزودن یا ویرایش دادهٔ زبانی

1. فایل `kolang-docs.json` را باز کنید.
2. شناسه و متن راهنمای فارسی کامل را به دستهٔ درست اضافه/ویرایش کنید:
   - `keywords` برای کلمات کلیدی
   - `builtins` برای توابع فراخوانی‌پذیر
   - `types` برای نوع‌های داده
   - `modules` برای ماژول‌ها
   - `exceptions` برای کلاس‌های خطا
   - `verbs` برای فعل‌های امری
   - `literals` برای مقادیر ثابت
3. متن راهنما باید کامل و گویا باشد و در صورت امکان مثال هم داشته باشد (مثل سایر ورودی‌ها).
4. شناسه‌های ترکیبی را با نیم‌فاصلهٔ دقیق (`U+200C`) بنویسید — مثل `خطای‌صفر` و `بسته‌است`.
5. همگام‌سازی را در مخزن kolang-vscode اجرا کنید:

```bash
./scripts/sync-vscode.sh
```

> همگام‌سازی برای `kolang-mobile` و `kolang-web` به‌زودی اضافه می‌شود.

6. خروجی اسکریپت‌ها را بررسی کنید (JS و JSON معتبر باشند) و سپس commit کنید.

## قواعد

- **هیچ‌وقت** فایل‌های داده را مستقیم در مخازن ویرایشگر ویرایش نکنید.
- **همهٔ** ورودی‌ها را در فایل متعارف نگه دارید — چیزی را حذف نکنید.
- متن فارسی را با نیم‌فاصلهٔ درست و اعداد فارسی بنویسید.
- ورودی‌ها را در هر دسته مرتب و خوانا نگه دارید.
- هر تغییر مهم را در [CHANGELOG.md](CHANGELOG.md) ثبت کنید.

## راه‌اندازی

فقط به `node` نیاز دارید (برای اسکریپت‌های همگام‌سازی). هیچ وابستگی دیگری وجود ندارد.

## گزارش اشکال و پیشنهاد

برای گزارش مشکل یا پیشنهاد ویژگی، یک issue در همین مخزن باز کنید.